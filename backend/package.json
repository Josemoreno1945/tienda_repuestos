{
  "name": "motorepuestos-backend",
  "version": "1.0.0",
  "description": "Backend API para plataforma de repuestos de motos",
  "main": "server.js",
  "scripts": {
    "start": "concurrently \"json-server --watch db.json --port 3001\" \"node server.js\"",
    "dev": "concurrently \"json-server --watch db.json --port 3001\" \"node server.js\"",
    "json-server": "json-server --watch db.json --port 3001"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "concurrently": "^8.2.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "json-server": "^0.17.4",
    "zod": "^3.23.0"
  }
}
